persistence:
  enabled: true
  storageClass: do-block-storage
  accessMode: ReadWriteOnce
  size: 100Gi

postgresql:
  username: relay
  database: relay-db
  existingSecret: postgresql

service:
  type: ClusterIP
  port: 5432

pgpool:
  tls:
    enabled: true
    autoGenerated: true
